fn get_name() : string {
    let in = input("Give me your name");
    if in != "matt" {
        panic("Not the correct name!");
    }
    return in;
}

let name = get_name();

print("name is: ", name);
